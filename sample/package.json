{
  "name": "xd-dialog-helper-sample",
  "version": "1.0.0",
  "description": "A sample plugin for the xd-dialog-helper (also used for testing the library)",
  "author": "Pablo Klaschka",
  "license": "MIT",
  "dependencies": {
    "xd-dialog-helper": "file:.."
  },
  "devDependencies": {
    "webpack": "^4.21.0",
    "webpack-cli": "^3.1.2",
    "@adobe/xdpm": "^1.1.2"
  },
  "scripts": {
    "build": "webpack --config webpack.config.js --mode development",
    "watch": "xdpm watch",
    "start": "npm install && npm run build && xdpm i -o"
  }
}
